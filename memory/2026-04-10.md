# 2026-04-10

## 双向同步重建
- 金嵩提供了新的PAT token，重新配置了 GitHub 远程仓库
- 执行了 merge：以远程仓库备份为准，合并了所有历史文件
- 验证了 `sync.sh` 的 push/pull 均正常工作
- 远程 URL 已内嵌 PAT token，后续同步无需额外配置

## 本地部署调试（OpenClaw + Ollama + 4060）
- Gateway 服务安装：`schtasks` 报错，改用 `openclaw gateway` 前台启动
- GPU 问题：Ollama 0.20.4/0.20.5 默认检测不到 4060，必须设 `OLLAMA_LLM_LIBRARY=cuda_v13` 才能走 CUDA
- 代理冲突：Clash 代理（127.0.0.1:7890）导致 GPU 检测失败，解法是设 `NO_PROXY` 排除 localhost
- 永久环境变量：`OLLAMA_LLM_LIBRARY=cuda_v13`，`NO_PROXY=localhost,127.0.0.1`
- 模型识别：Ollama `/v1/models` 返回的 ID 带 `:latest` 后缀，OpenClaw 配置必须写 `alibayram/mimo-7b-rl:latest` 才能匹配
- 最终状态：4060 GPU 走 CUDA v13 推理，OpenClaw webchat 可用，gateway 启动约 10 秒
